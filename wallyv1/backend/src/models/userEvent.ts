// Minimal UserEvent model for compatibility
export class UserEvent {
  static async create(data: any): Promise<void> {
    // Implement DB logic or mock as needed
  }
  static async findAll(options: any): Promise<any[]> {
    // Implement DB logic or mock as needed
    return [];
  }
}
